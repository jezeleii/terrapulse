# PostgreSQL connection string (required for VCM analytics data)
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# Mapbox token for the project map (required — get from mapbox.com)
NEXT_PUBLIC_MAPBOX_TOKEN="pk.eyJ1Ii..."

# Google Gemini API key for AI news generation (required — get from aistudio.google.com)
GEMINI_API_KEY="AIza..."
